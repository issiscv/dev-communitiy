spring:
  datasource:
    url: jdbc:mysql://mysql:3305/board?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 369369rt
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true -> 프린트
        ##use_sql_comments : true -> jpql
        format_sql: true
        default_batch_fetch_size: 100

logging.level:
  org.hibernate.SQL: debug